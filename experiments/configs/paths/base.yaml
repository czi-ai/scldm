# Dataset and release roots
# TODO: Update path - these are relative paths to local artifact directories
base_data_path: _artifacts/datasets
base_release_path: _artifacts/
base_experiment_path: experiments/
